<StackLayout class="camera-section p-4">
    <Button text="📸 Take Photo" tap="{{ onTakePhoto }}" class="btn-primary" />
    <Label text="Tap to scan your fridge ingredients" class="text-secondary text-center mt-2" />
</StackLayout>